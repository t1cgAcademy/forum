import React from 'react';
import './history.css';

// shorten a message if it excedes a certain length
const preview = (str) => {
  // TODO
}

// Generate a list of posts that consist of a title and a summary
// of the message. An element of the list should be set to the current
// post when clicked. Also, add a "New Post" button here.
const History = props => {
  return (
    <div>
      History
    </div>
  );
};

export default History;
